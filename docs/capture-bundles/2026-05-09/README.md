# Yang-Mills Program — Capture Bundle 2026-05-09

This directory records the repo-side ingest of the 2026-05-09 Yang-Mills capture bundle.

The capture adds six review surfaces:

1. Recovered manuscript text extracts from nine formerly zip-disguised PDF project files.
2. Phase 2 `Jmax = 1/2` fixture verification.
3. Phase 3a `Jmax = 1` basis registry and sector labeling.
4. Phase 3b `Jmax = 1` Gram matrix audit.
5. Phase 3c `Jmax = 1 -> Jmax = 1/2` truncation regression.
6. Methodology Manifesto v0.2.

The source bundle also contained reconstructed PDFs. Repository policy treats PDFs as distribution artifacts, so this capture commits the searchable text extracts and records PDF checksums in `artifact-manifest.json` for later binary-safe storage.

See `ingest-report.md` for normalization, validation commands, and claim boundaries.
